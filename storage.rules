rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Only signed-in users may upload or download files.
      allow read, write: if request.auth != null;
    }
  }
}
