# Firebase Admin SDK service account JSON path
FIREBASE_CREDENTIALS_PATH=./serviceAccountKey.json

# Firebase project config (for storage bucket)
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Whisper model size: tiny | base | small | medium | large
WHISPER_MODEL=base

# Summarization model: facebook/bart-large-cnn | t5-base
SUMMARIZER_MODEL=facebook/bart-large-cnn

# Set to true to use T5-small for summarization instead of extractive method
USE_AI_SUMMARIZER=false

# ── AI / LLM Features ────────────────────────────────────────────────────────
# Groq API key — PRIMARY LLM for summarization, quiz generation, transcript cleaning.
# Get yours at: console.groq.com/keys
GROQ_API_KEY=
# Optional fallback key — used automatically if the primary hits a rate-limit / quota error.
GROQ_API_KEY_2=
GROQ_MODEL=qwen/qwen3-32b

# Google Gemini API key — used ONLY for PDF OCR + image-based multimodal quiz (vision).
# Get yours at: aistudio.google.com/app/apikey
GEMINI_API_KEY=
